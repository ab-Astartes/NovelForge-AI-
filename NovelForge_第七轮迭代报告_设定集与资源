# NovelForge（墨阁）第七轮迭代报告 —— 设定集抽取 + 资源战力账本

> 日期：2026-08-31 ｜ 模块：设定集/术语表自动抽取 · 资源与战力账本
> 承接第六轮（P5）竞品对标路线图第 1、4 项：LoreWeave 的术语表自动抽取、inkos/StoryWeaver 的资源账本。

---

## 一、为什么要做这两个功能

第六轮对标 GitHub 同类产品后，功能矩阵里墨阁唯一明确缺失、且竞品壁垒最高的两项是：

| 缺口 | 竞品状态 | 价值 |
|---|---|---|
| 术语表/设定集自动抽取 | LoreWeave ✅（RAG glossary） | 长篇连载最容易崩的是设定一致性；自动抽取可把「写了 80 章后自己都忘了的设定」显性化 |
| 资源与战力账本 | inkos ✅、StoryWeaver ✅ | 「主角灵石花不完」「战力膨胀」是网文两大硬伤，需要机器核验而非人肉回忆 |

二者都沿用墨阁一贯的**零 LLM 成本**策略（正则 + 词表 + 统计），与关系图谱、疆域地图、智能取名同一技术路线。

---

## 二、功能一：设定集 / 术语表自动抽取

接口：`GET /api/glossary?path=<书目录绝对路径>`

### 2.1 六类实体抽取

| 类型 | 抽取策略 |
|---|---|
| 功法典籍 | 《》书名号（最高置信）+ `诀/经/大法/剑法/心法/真解/宝典/神通…` 后缀 |
| 法宝兵器 | `剑/刀/塔/印/炉/鼎/珠/符/丹/甲/葫芦…` 后缀，前缀 1-5 字 |
| 地名 | `镇/城/山/谷/郡/州/府/域/界/渊/殿/宫/阁/楼…` 后缀 |
| 境界体系 | 修真（炼气→渡劫→仙帝）、武道（武者→武帝）、斗气（斗者→斗帝）三大体系 13 阶 + `层/重/段/阶/品` |
| 人物 | 说话动词（说道/问道/冷哼/喃喃…）前 2-4 字 + 角色表已登记人物 |
| 术语 | 定义句模式：`X，是/即/指的是/被称为/谓之/称作…` 自动抓释义 |

**降噪设计**：内置 60+ 泛词黑名单（`长剑/手掌/世界/时间…`），避免通用词污染；未登记实体需满足「出现 ≥2 次」或已在角色表/世界观登记才入选。

### 2.2 三类一致性告警

1. **高频未登记**（warn）—— 出现 ≥3 次但不在角色表/世界观中，提示补入设定集
2. **疑似异写**（warn）—— 同长、仅差一字、共用尾字（如「混沌诀 / 混元诀」），提示同一设定的两种写法
3. **低频孤词**（info）—— 仅出现 2 次且长度 ≥3，提示可能为笔误或一次性名词

### 2.3 前端（glossary.js）

- 类型 chips 带实时计数 + 「仅未登记」快筛 + 名称/释义搜索
- 实体卡展示词频、首现章节、释义与 3 条例句，未登记者打红色徽标
- **导出设定集 Markdown**：按类型分章节，含释义/例句/告警，可直接作为作品设定集交付

---

## 三、功能二：资源与战力账本

接口：`GET /api/ledger?path=<书目录绝对路径>`

### 3.1 资源收支抽取

- **22 种资源 × 计量单位**：灵石（含极品/上品/中品/下品）、灵晶、金币、银两、贡献点、积分、声望、丹药、符箓、灵草、妖丹、寿元…
- **中文数字解析**：支持 `三百块灵石`、`一万二千`、`3.5` 等写法（十/百/千/万/亿 进位）
- **收支判定**：取数量前 14 字窗口，命中收入动词（获得/赏赐/赚取/换取…）记为正，支出动词（花费/消耗/支付/赔偿…）记为负
- **余额结算**：按章顺序累加，输出完整流水

### 3.2 境界进阶追踪

- 句中同时出现「已登记角色名 + 境界词」才记录归属（避免无主境界噪声）
- 生成每人境界时间线（去重相邻重复），用于绘制进阶曲线

### 3.3 三类账目告警

1. **凭空支出**（error）—— 支出后余额转负，说明前文没有对应收入
2. **战力膨胀**（warn）—— 单章跨阶 > 2（如炼气三层 → 元婴）
3. **数值无上下文**（info）—— 数量反复出现但无收支动词，未计入流水（提示改用「获得/花费 N 灵石」句式）

### 3.4 手工账

支持 `truth/ledger.json`（数组或 `{entries:[...]}`）补充正文未捕获的账目，字段：`resource / delta / chapter / note`，合并进结算并标注来源为「手工账」。

### 3.5 前端（ledger.js）

- 三个标签页：资源余额卡 / 境界进阶柱图 / 告警列表
- 余额卡按中国习惯配色（结余为正=红、透支=绿），含收/支/提及次数
- 流水表最多 200 行，带上下文原文；**导出 CSV**（含 BOM，Excel 直接打开）

---

## 四、实现要点与坑

1. **面板 id 冲突**：`panel-ledger` / `nav-ledger` / `ledger-book` 已被 Truth 状态「台账」面板占用，新面板整体改名为 `assets`（函数 `loadAssets` / `exportAssets`，DOM 前缀 `assets-`）。
2. **`Map.of` 上限 10 对**：中文数字映射有 11 项（含「两」「零」），改用静态 `LinkedHashMap` 初始化。
3. **统一鉴权入口**：新增面板一律走 `authUrl(API + ...)` + `authHeaders()`；顺手修掉 naming.js 里遗留的裸 `fetch('/api/naming')`。
4. **共享下载函数**：`downloadFile(content, filename, mime)` 定义在 app.js，设定集 MD 与账本 CSV 共用（避免审计 [C] 重复定义）。
5. **静态资源白名单**：新 JS 必须在 `StudioServer` 的 `serveResource` 分支注册，否则 404（第六轮踩过的坑，本轮预先注册）。

---

## 五、验证

- 前端审计：HTML ids 279 / JS 引用 245，**[A] 缺失 id 0 · [B] 未定义函数 0 · [C] 重复函数 0 · [D] 缺失 class 0 · [E] 面板↔导航 14↔14 全匹配**
- `node --check` × 6 个 JS 全部通过
- Maven `-o test`：BUILD SUCCESS（270 基线 + StudioServerTest 8 项，0 failures / 0 errors）
- Studio 冒烟：对真实书目跑 `/api/glossary` 与 `/api/ledger`，实体抽取、收支结算、告警输出正常

---

## 六、文件改动清单

- 新增后端：`GlossaryBuilder.java`、`LedgerBuilder.java`（studio 包）
- 新增前端：`glossary.js`、`ledger.js`
- 改动：`StudioServer.java`（2 路由 + 2 handler + 静态资源白名单 ×2）、`index.html`（2 导航 + 2 面板 + 2 script）、`app.js`（PANEL_MAP ×2、书下拉 ×2、showPanel 分支 ×2、`downloadFile` 工具）、`style.css`（设定集 + 账本样式）、`.audit/checkids.js`（纳管新文件）
- 文档：本报告 + README 补两节

---

## 七、下一步候选

- 报告第六轮路线图余项：情绪张力曲线与节拍器、守护进程后台日更、分支剧情/互动小说、多语种翻译工作台
- 设定集增强：实体合并（异写一键归并）、设定卡编辑回写 `truth/glossary.json`、与图谱/疆域联动（点实体高亮相关章节）
- 账本增强：战力数值曲线（非境界词时的数值型战力）、按角色分账、与审计引擎联动（33 维审计新增「资源一致性」维度）
