package com.novelforge.cli.commands;

/**
 * ExportCommand — export book to EPUB, TXT, or Markdown format.
 */
public class ExportCommand {

    public void execute(String[] args) {
        // TODO: 1. Parse --format (epub/txt/md), --output options
        //       2. Load all chapters from book
        //       3. Convert to target format
        //       4. Write output file
    }
}
