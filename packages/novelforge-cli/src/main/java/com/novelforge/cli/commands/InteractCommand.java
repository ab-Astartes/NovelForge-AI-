package com.novelforge.cli.commands;

/**
 * InteractCommand — start interactive dialogue mode with the writing engine.
 * Natural language conversation for real-time writing guidance.
 */
public class InteractCommand {

    public void execute(String[] args) {
        // TODO: 1. Load book + truth state
        //       2. Start REPL loop
        //       3. Parse user intent (write, revise, audit, ask about characters, etc.)
        //       4. Route to appropriate pipeline or query
        //       5. Display results, continue loop
    }
}
