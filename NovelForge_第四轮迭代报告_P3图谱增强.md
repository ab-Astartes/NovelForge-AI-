# NovelForge 第四轮迭代报告：P3 图谱增强（势力包围圈 + 三扩展收口）

> 项目：`C:\Users\13631\Desktop\ab\demo\NovelForge`
> 日期：2026-08-27
> 驱动诉求：继续完成后续功能、补齐差异、优化功能和页面

---

## 一、本轮交付总览

| # | 交付项 | 类型 | 状态 |
|---|--------|------|------|
| 1 | **势力包围圈（convex hull + 胶囊）** | P2 图谱扩展候选 | ✅ 完成（本轮新增） |
| 2 | 势力着色聚类（同势力同色系 + 质心引力 + 图例） | P2 图谱扩展候选 | ✅ 完成（收口） |
| 3 | 章节演变动画（逐帧点亮新增关系 + 橙色脉冲） | P2 图谱扩展候选 | ✅ 完成（收口） |
| 4 | 节点过滤（类型 chips / 提及数阈值 / 搜索） | P2 图谱扩展候选 | ✅ 完成（收口） |
| 5 | README 图谱能力说明补全（含包围圈） | 文档 | ✅ 完成 |

**竞品覆盖率：13/13 保持闭环**，P2 关系图谱扩展三候选全部落地，图谱从"能看"升级到"能读懂阵营、能看演变、能精确过滤"。

---

## 二、势力包围圈（本轮核心新增）

第三轮已实现势力推断 + 同色系着色 + 力模拟质心引力，但画布上阵营边界仍靠"眼力"分辨。本轮补上**包围圈可视化**：

### 算法
- **凸包**：Monotone Chain（单调链），O(n log n)，返回逆时针顶点序列
- **≥3 名成员**：凸包顶点按中心向外扩张 16px，二次贝塞尔中点法生成**平滑闭合曲线**
- **恰好 2 名成员**：两点胶囊路径（两端半圆 + 直线段），避免凸包退化成一条线
- 边界单测：方形→4 点、三角+内部点→3 点、共线→2 点、两点→胶囊，全部通过

### 渲染
- 绘制层次：背景 → **包围圈层** → 边层 → 边标签层 → 节点层（不遮挡连线与节点）
- 样式：势力色半透明填充（10%）+ 虚线描边（38%），`pointer-events: none` 不拦截拖拽/点击
- 每帧随力模拟重建，节点拖动时包围圈实时跟随
- 高亮某节点时，非其所属势力的包围圈自动降透明（0.22），聚焦感更强
- 势力名标签显示在包围圈几何中心上方，图例仍保留成员计数

---

## 三、三扩展收口（第三轮候选 → 本轮验收）

| 扩展 | 实现细节 | 验收 |
|------|----------|------|
| **势力着色聚类** | 显式 faction > world.json 命中 > 正则启发式 + 后缀归并（「青阳镇萧家」→「萧家」）；同势力同色系 + 质心引力 0.006 聚拢 | 苍穹破：萧尘/萧风自动归入「萧家」，色系一致 |
| **章节演变动画** | 后端按章序输出首次出现的关系边 `chapters:[{index,title,added:[{source,target,chapter}]}]`（上限 60 章）；前端播放条：▶ 演变播放 / ■ 复位 / 拖动滑块；未出现边淡化、当前章新增边橙色脉冲 | 苍穹破 3 章扫描出 1 章新增关系，播放逻辑正常 |
| **节点过滤** | 类型 chips（人物/地点/势力/物品/体系/规则）、提及数滑块（0-50）、名称/描述搜索；过滤结果实时统计可见关系数与"过滤 X/Y 节点"提示 | 过滤隐藏的节点不参与包围圈与边渲染 |

---

## 四、验证结果

| 验证项 | 结果 |
|--------|------|
| Maven 全模块 compile | ✅ 通过 |
| `mvn test` | ✅ **270 tests / 0 failures / 0 errors / 0 skipped**（262 核心 + 8 Studio） |
| 前端审计 checkids | ✅ [A]=0 [B]=0 [C]=0 [D]=0，10 面板 ↔ 10 导航 0 缺口 |
| 前端审计 checkhtml | ✅ [F]=0 [G]=0 |
| 前端审计 checkcss | ✅ [I]=0 [J]=0 |
| JS 语法检查 | ✅ app.js / graph.js `node --check` 通过（含新增 hull 函数） |
| 凸包算法单测 | ✅ 方形/三角/共线/两点四种边界正确 |
| Studio 启动（:8964 --no-auth） | ✅ 运行正常 |
| `/api/graph`（苍穹破） | ✅ 萧尘[萧家] 萧风[萧家] 势力归属正确；敌对关系标注；chapters 演进输出 |
| `/api/graph`（越界路径） | ✅ 400 拒绝 |
| `/api/graph`（空数据书） | ✅ 优雅降级 `{ok:true, nodes:[], …}` |
| 静态资源 | ✅ index / app.js / graph.js / style.css 全部 200 |

---

## 五、图谱实测样例（苍穹破 · 本轮）

```
节点：萧尘[萧家](97提及·度3)  萧风[萧家](25·3)  萧灵儿(8·3)  老者(36·3)
边：  萧尘↔萧灵儿 w4 | 萧尘↔萧风 w4 [敌对] | 萧尘↔老者 w2
      萧灵儿↔萧风 w2 | 老者↔萧风 w2 | 老者↔萧灵儿 w2
包围圈：「萧家」2 名成员（萧尘、萧风）自动圈出胶囊包围圈，阵营一眼可辨
```

---

## 六、遗留观察与下一步建议

- **图谱**：
  - 关系标签支持自定义/编辑（当前为纯启发式推断，个别边可能误判）
  - 按势力「只显示该势力子图」的快捷过滤
  - 章节播放支持倍速与自动暂停在关键转折章
- **多用户 / PG 化**：需重新设计 schema（此前已列，本轮未动）
- **StudioServer 离线 LLM / Ollama 路由**：可补本地推理链路
- **开源准备**：与 RhythmicWave/NovelForge 同名，README 已强调「墨阁」品牌区分

## 附：竞品对标闭环（13/13）

| 对标能力 | 状态 |
|---------|------|
| 长程记忆（RAG 语义召回 + 词面降级） | ✅ 第二轮 |
| 完本导出 docx/pdf/epub/txt/md | ✅ 第二轮 |
| 风格克隆 / Webhook / 封面 / 市场雷达 | ✅ 第二轮 |
| 关系图谱可视化 | ✅ 第三轮 |
| **图谱增强（势力包围圈 / 章节演变 / 节点过滤）** | ✅ **本轮** |
