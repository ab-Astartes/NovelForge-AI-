# NovelForge 第三轮迭代报告：P2 关系图谱 + 页面优化

> 项目：`C:\Users\13631\Desktop\ab\demo\NovelForge`
> 日期：2026-08-27
> 驱动诉求：继续完成后续功能、补齐差异、优化功能和页面

---

## 一、本轮交付总览

| # | 交付项 | 类型 | 状态 |
|---|--------|------|------|
| 1 | `/api/graph` 关系图谱聚合接口（后端） | P2 差异化功能 | ✅ 完成 |
| 2 | 「图谱」面板：力导向 SVG 关系网络（前端） | P2 差异化功能 | ✅ 完成 |
| 3 | 前端模块化拆分：`graph.js` 独立模块 | P2 候选（顺带落地） | ✅ 完成 |
| 4 | 快捷键升级 Ctrl+1~0、台账→图谱快捷入口 | 页面优化 | ✅ 完成 |
| 5 | PANEL_MAP 重复 key 清理、审计脚本双文件扫描 | 代码优化 | ✅ 完成 |

**竞品覆盖率：12/13 → 13/13**（P2 关系图谱是 6 个对标竞品全部缺失的差异化领先点，本轮补齐后全部对标项闭环）。

---

## 二、后端：`/api/graph` 关系图谱接口

### 数据来源（TruthState 三源聚合 + 章节正文）
1. **人物节点**：`truth/characters.json` → `{id, label, group=character, desc, role}`
2. **场景/势力节点**：`truth/world.json` 的 locations / factions / items / systems / rules → 各自 group
3. **关系边 · 时间线**：`truth/timeline.json` 每条事件描述中的人物共现 → 加权边
4. **关系边 · 章节正文**：扫描 `chapters/*.md`（跳过 `.draft.`），章内共现 + 提及次数统计

### 关系标签推断（启发式，零 LLM 成本）
事件描述命中关键词 → 边标注关系类型，优先级：敌对 > 师徒 > 亲情 > 爱慕 > 友盟。
示例：`"萧风带人找萧尘麻烦，被萧尘击败"` → 萧尘↔萧风 边自动标注 **敌对**。

### 输出结构
```json
{
  "ok": true, "book": "苍穹破",
  "nodes": [{ "id":"萧尘", "group":"character", "mentions":97, "degree":3, "desc":"…" }],
  "edges": [{ "source":"萧尘", "target":"萧风", "weight":4, "label":"敌对" }],
  "stats": { "characters":4, "worldEntities":0, "events":11, "edges":6 }
}
```
- 无向边按名字字典序去重（`a\u0000b` key），权重累加；排序后截断 Top 400 条保证渲染性能
- 空数据书 → `{ok:true, nodes:[], edges:[], stats:0}` 优雅降级；路径越界 → 400 拒绝

### 顺带修复
`TimelineState` 补 `getData()` 访问器（与 `WorldState` 对齐，供图谱/导出工具使用）。

---

## 三、前端：「图谱」面板（零外部依赖力导向布局）

### 页面结构（`index.html`）
- 侧栏新增 `nav-graph`（🕸 图谱），位于台账与工具箱之间 → **10 面板 ↔ 10 导航**
- 工具栏：书目下拉 + 刷新 + 重新布局 + 统计（N 人物 · M 场景/势力 · K 关系）
- 画布区：SVG 力导向图 + 悬停 Tooltip
- 侧栏卡片：图例（人物/地点/势力/物品体系）+ 核心人物 Top10（按 degree 排序，点击聚焦）

### 交互能力（`graph.js`，约 500 行，纯 vanilla）
| 交互 | 说明 |
|------|------|
| 力导向布局 | 斥力 O(n²) + 弹簧（边权重→拉力）+ 向心力 + 阻尼，rAF 收敛 |
| 节点拖拽 | 拖后固定（pinned），不参与后续力模拟 |
| 悬停提示 | 节点：角色/描述/提及次数/关联数；边：共现次数+关系类型 |
| 点击高亮 | 聚焦某节点 → 其余节点降透明，相关边加粗 |
| 滚轮缩放 | 以光标为中心 0.35x~3x |
| 空白拖拽 | 平移画布（视口变换） |
| 核心光环 | degree ≥ 4 的节点带外圈光环，一眼识别核心人物 |
| 关系标签 | 共现 ≥2 次且有推断类型的边显示中文标签（敌对/师徒/…） |

### 模块化拆分
图谱全部逻辑独立到 `graph.js`（app.js 从 3792 行不再膨胀），`index.html` 按顺序加载 app.js → graph.js；审计脚本 `checkids.js` 升级为双文件联合扫描（[B] 项 0 缺口验证通过）。

---

## 四、页面与代码优化

1. **快捷键升级**：Ctrl+1~7 → **Ctrl+1~0**（0=设置），帮助面板文本同步为 10 面板全量清单
2. **台账→图谱快捷入口**：台账面板头部「🕸 查看关系图谱」按钮，自动带当前书目跳转
3. **PANEL_MAP 去重**：删除重复的 `'progress': 'toolbox'` 键（此前被后置 `'progress':'progress'` 静默覆盖）
4. **图谱下拉复用**：`graph-book` 加入 `populateBookSelects` 统一书目同步（自动沿用全局选书）
5. **按钮类名规范**：新按钮改用 `btn-ink`/`btn-ghost btn-sm`，弃用无定义的 `btn-primary`/`btn`（[D] 项归零）
6. **窗口自适应**：resize 时重算 viewBox 并重绘

---

## 五、验证结果

| 验证项 | 结果 |
|--------|------|
| Maven 全模块 compile | ✅ 通过 |
| `mvn test` | ✅ **270 tests / 0 failures / 0 errors / 0 skipped** |
| 前端审计 checkids | ✅ [A]=0 [B]=0 [C]=0 [D]=0，10 面板 ↔ 10 导航 0 缺口 |
| 前端审计 checkhtml | ✅ [F]=0 [G]=0（标签闭合/重复属性无问题） |
| 前端审计 checkcss | ✅ [I]=0 [J]=0（未声明变量 0、真重复选择器 0） |
| JS 语法检查 | ✅ app.js / graph.js `node --check` 通过 |
| Studio 启动（:18999 --no-auth） | ✅ 运行正常 |
| `/api/graph`（苍穹破：4 角色 11 事件 3 章） | ✅ 4 节点 6 边，提及/度数正确，**敌对关系自动标注** |
| `/api/graph`（空数据书） | ✅ 优雅降级 `{ok:true, nodes:[], …}` |
| `/api/graph`（路径越界） | ✅ 400 拒绝 |
| 静态资源 | ✅ `/graph.js` 200 (16.7KB) / app.js 200 / index.html 含图谱面板 |

---

## 六、图谱实测样例（苍穹破）

```
节点：萧尘(提及97·度3)  老者(36·3)  萧风(25·3)  萧灵儿(8·3)
边：  萧尘↔萧灵儿 w4 | 萧尘↔萧风 w4 [敌对] | 萧尘↔老者 w2
      萧灵儿↔萧风 w2 | 老者↔萧风 w2 | 老者↔萧灵儿 w2
```
主角萧尘自动成为网络中心，与萧风的对抗关系被正确识别为「敌对」，与萧灵儿/老者的关系为高频共现——与正文设定一致。

---

## 七、遗留观察与下一步建议

- **图谱扩展候选**：
  - 势力着色聚类（同势力节点同色系 + 势力包围圈）
  - 章节播放动画（按章节推进高亮新增关系，可视化"关系如何演变"）
  - 节点过滤（仅人物 / 含场景、按提及数阈值）
- **多用户 / PG 化**：需重新设计 schema（此前已列，本轮未动）
- **StudioServer 离线 LLM / Ollama 路由**：可补本地推理链路
- **文档**：README 补充「图谱」能力说明（开源前品牌区分注意：与 RhythmicWave/NovelForge 同名，强调「墨阁」）

## 附：竞品对标闭环（13/13）

| 对标能力 | 状态 |
|---------|------|
| 长程记忆（RAG 语义召回 + 词面降级） | ✅ 上轮 |
| 完本导出 docx/pdf/epub/txt/md | ✅ 上轮 |
| 风格克隆 / Webhook / 封面 / 市场雷达 | ✅ 上轮 |
| **关系图谱可视化** | ✅ **本轮** |
